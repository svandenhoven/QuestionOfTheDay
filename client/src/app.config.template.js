// Application Config
const config = {
    fluidrelay: {
      tenantId: 'fluid relay tenant id',
      tokenProviderUrl: 'token provider endpoint',
      endpoint: 'fluid replay endpoint'
    }
   };
   
   export default config;