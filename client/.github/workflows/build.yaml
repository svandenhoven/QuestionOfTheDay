name: build question of the day client
run-name: ${{ github.actor}} is building question client
on: [push]
jobs:
  build-question-client:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          node-version: 16
      - run: npm install
      - run: npm run build